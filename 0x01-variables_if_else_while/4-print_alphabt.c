#include <stdio.h>
/**
 * main - Entry point.
 *
 * Return: 0 indicate success.
 */
int main(void)
{
	for(int a = 'a' ; a <= 'z' ; a++)
	{
		putchar(a);
		while(;a == 101 || a == 113)
			a++;
	}
	putchar('\n');
	return (0);
}
