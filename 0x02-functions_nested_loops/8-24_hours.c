#include "main.h"
#include <stdio.h>

void jack_bauer(void)
{
	int h ,m;

	for (h = '0' ; h <= '9' ; h++)
		pucthar(h);
		 for (h = '0' ; h <= '9' ; h++)
		 {
			 {
				 pucthar(h);
				 for (m = '0' ; m <= '9' ; m++)
				 {
					 putchar(m);
					 for (m = '0' ; m <= '9' ; m++)
					 {
						 putchar(m);
					 }
				 }
			 }
		 }

}
